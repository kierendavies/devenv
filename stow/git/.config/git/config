[core]
    editor = nvim

[pull]
    ff = only

[rebase]
    autoSquash = true
    autoStash = true

[user]
    useConfigOnly = true

[include]
    path = alias.conf
    path = user.conf

    # Enable environment-specific overrides, e.g. to use a work email address,
    # without modifying any versioned files. This must be the last declaration
    # in this file.
    path = override.conf
